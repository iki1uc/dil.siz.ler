name: dil-siz-ler-Final

on:
  workflow_run:
    workflows: ["iki2li-Identitaet"]
    types: [completed]

jobs:
  final:
    runs-on: ubuntu-latest
    steps:
      - name: Finalisierung an OP
        run: echo "dil.siz.ler → OP: Final-Respo abgeschlossen"
