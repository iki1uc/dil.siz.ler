root: dil.siz.ler

matrix:
  - [dil, siz, ler]
  - [rew, me,  for]
  - [gen, tol, ech]

dil:
  mode: open
  axis: start

siz:
  mode: cut
  axis: line

ler:
  mode: echo
  axis: return

run:
  full: true
  rotation: forward
  echo: enabled
