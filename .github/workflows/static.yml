name: dil-siz-ler-Final

on:
  workflow_run:
    workflows: ["iki2li-Identitaet"]
    types: [completed]

jobs:
  final:
    runs-on: ubuntu-latest
    steps:
      - name: dil.siz.ler empfängt Kern-ID
        run: echo "dil.siz.ler: Kern-ID empfangen"

      - name: dil.siz.ler OK + VERIFY
        run: echo "dil.siz.ler: OK – VERIFY – Dienst aktiv"
